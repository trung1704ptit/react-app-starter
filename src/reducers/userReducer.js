import * as actions from './constants';

const initialState = {
    profile: {
        access_token: ''
    }
};

const system = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default system;
